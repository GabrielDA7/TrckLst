const URI = 'http://localhost:3000';

const LANDING = '/';
const SPOTIFY_AUTH = '/spotify-auth'
const CREATE_ROOM = '/create-room';
const ROOM = '/room';

export default {
    URI,
    LANDING,
    SPOTIFY_AUTH,
    CREATE_ROOM,
    ROOM,
}